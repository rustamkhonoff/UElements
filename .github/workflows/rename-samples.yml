name: Rename Samples folder

on:
  workflow_dispatch:

  push:
    branches:
      - develop
    paths:
      - "Assets/src/UIElements.OdinInspector/Samples/**"
      - "Assets/src/UIElements.OdinInspector/Samples.meta"
      - ".github/workflows/rename-samples.yml"

permissions:
  contents: write

jobs:
  rename-samples:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Rename Samples to Samples~
        shell: bash
        run: |
          OLD_PATH="Assets/src/UIElements.OdinInspector/Samples"
          NEW_PATH="Assets/src/UIElements.OdinInspector/Samples~"

          OLD_META="Assets/src/UIElements.OdinInspector/Samples.meta"
          NEW_META="Assets/src/UIElements.OdinInspector/Samples~.meta"

          if [ -d "$OLD_PATH" ] && [ ! -d "$NEW_PATH" ]; then
            git mv "$OLD_PATH" "$NEW_PATH"
          fi

          if [ -f "$OLD_META" ] && [ ! -f "$NEW_META" ]; then
            git mv "$OLD_META" "$NEW_META"
          fi

          if git diff --cached --quiet; then
            echo "Nothing to rename"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git commit -m "Rename Samples to Samples~ [skip ci]"
          git push
